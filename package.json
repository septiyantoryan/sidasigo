{
  "name": "sidasi-go",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "dev": "turbo dev --parallel",
    "dev:frontend": "turbo dev --filter=sidasi-go-frontend",
    "dev:backend": "turbo dev --filter=sidasi-go-backend",
    "build": "turbo build",
    "test": "turbo test --concurrency=1"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
